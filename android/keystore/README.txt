This PKCS12 is the stable update-signing key for Self Agent APKs.
Keep it so later GitHub Actions builds can install over previous ones.
Do not replace it unless you are willing to uninstall the app on every phone.
