# Quarantined old frontend

This tree was the previous `src/` router/routes UI.

It is not imported by `app/page.tsx`, `static-web/main.tsx`, or the Android WebView assets pipeline. Kept for reference only; do not restore without proving a live entrypoint.
